[package]
name = "typforge"
version = "0.1.0"
edition = "2021"

[dependencies]
axum = "0.8.1"
tokio = {version = "1.43.0", features = ["rt-multi-thread"]}
tower-http = {version = "0.6.2", features = ["fs"]}
